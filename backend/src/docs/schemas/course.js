// Course + CoursePlan schema
module.exports = {
  Course: {
    type: 'object',
    properties: {
      _id: { type: 'string', example: '60d5dbf5d74b8c3b44b8b4c3' },
      instructorId: { type: 'string', example: '60d5dbf5d74b8c3b44b8b4c1' },
      title: { type: 'string', example: 'Guitar cơ bản' },
      description: { type: 'string', nullable: true },
      thumbnail: { type: 'string', nullable: true },
      isFree: { type: 'boolean', example: false },
      courseType: { type: 'string', example: 'foundation' },
      level: { type: 'string', enum: ['beginner', 'intermediate', 'advanced'], example: 'beginner' },
      status: { type: 'string', enum: ['pending', 'published', 'archived'], example: 'published' },
      adminCommissionPercentage: { type: 'number', example: 30 },
      tags: { type: 'array', items: { type: 'string' } },
      totalLessons: { type: 'integer', example: 12 },
      enrollCount: { type: 'integer', example: 0 },
      isFeatured: { type: 'boolean', example: false },
      publishedAt: { type: 'string', format: 'date-time', nullable: true },
      plan: {
        nullable: true,
        description: 'Gói giá liên kết (CoursePlan). null nếu chưa đặt giá. Public chỉ thấy khi isActive.',
        $ref: '#/components/schemas/CoursePlan',
      },
      createdAt: { type: 'string', format: 'date-time' },
      updatedAt: { type: 'string', format: 'date-time' },
    },
  },
  CreateCourseInput: {
    type: 'object',
    required: ['title', 'level'],
    description: 'KHÔNG có giá — đặt giá riêng qua PUT /courses/{id}/plan',
    properties: {
      title: { type: 'string', example: 'Guitar cơ bản' },
      description: { type: 'string' },
      thumbnail: { type: 'string' },
      isFree: { type: 'boolean', example: false },
      courseType: { type: 'string', example: 'foundation' },
      level: { type: 'string', enum: ['beginner', 'intermediate', 'advanced'] },
      status: { type: 'string', enum: ['pending', 'published', 'archived'] },
      adminCommissionPercentage: { type: 'number', minimum: 0, maximum: 100, example: 30 },
      tags: { type: 'array', items: { type: 'string' } },
      isFeatured: { type: 'boolean' },
    },
  },
  UpdateCourseInput: {
    type: 'object',
    description: 'Đổi status sang published/archived sẽ tự bật/tắt isActive của CoursePlan',
    properties: {
      title: { type: 'string' },
      description: { type: 'string' },
      thumbnail: { type: 'string' },
      isFree: { type: 'boolean' },
      courseType: { type: 'string' },
      level: { type: 'string', enum: ['beginner', 'intermediate', 'advanced'] },
      status: { type: 'string', enum: ['pending', 'published', 'archived'] },
      adminCommissionPercentage: { type: 'number', minimum: 0, maximum: 100 },
      tags: { type: 'array', items: { type: 'string' } },
      isFeatured: { type: 'boolean' },
    },
  },
  CoursePlan: {
    type: 'object',
    description: 'Gói đăng ký (giá) của một course — bán theo chu kỳ.',
    properties: {
      _id: { type: 'string', example: '60d5dbf5d74b8c3b44b8b4d0' },
      courseId: { type: 'string', example: '60d5dbf5d74b8c3b44b8b4c3' },
      name: { type: 'string', nullable: true, example: 'Gói tháng' },
      description: { type: 'string', nullable: true },
      price: { type: 'number', example: 199000 },
      currency: { type: 'string', enum: ['VND', 'USD'], example: 'VND' },
      billingCycle: { type: 'string', enum: ['monthly', 'quarterly', 'yearly'], example: 'monthly' },
      features: { type: 'array', items: { type: 'string' } },
      isActive: { type: 'boolean', example: true, description: 'Tự bật khi course published' },
    },
  },
  UpsertCoursePlanInput: {
    type: 'object',
    required: ['price', 'billingCycle'],
    properties: {
      price: { type: 'number', minimum: 0, example: 199000 },
      billingCycle: { type: 'string', enum: ['monthly', 'quarterly', 'yearly'], example: 'monthly' },
      name: { type: 'string', example: 'Gói tháng' },
      description: { type: 'string' },
      features: { type: 'array', items: { type: 'string' } },
    },
  },
};
